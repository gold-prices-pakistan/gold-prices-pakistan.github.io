name: Scrape City Gold Data

on:
  # schedule:
  #   - cron: "15 7 * * *"  # 7:15 AM UTC daily
  #   - cron: "15 13 * * *" # 1:15 PM UTC daily
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape-city:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          pip install requests beautifulsoup4

      - name: Create Data Directory
        run: mkdir -p data

      - name: Run City Data Scraper
        run: |
          cd scripts
          python scraper-city.py
          mv scraper-city-data.json ../data/scraper-city-data.json

      - name: Commit & Push Changes
        run: |
          git config user.name "goldpk-bot"
          git config user.email "thesaadarshad+goldpk@gmail.com"
          git add -f data/scraper-city-data.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Auto Update: City Gold Data Scrape" || echo "Nothing to commit"
          git push

